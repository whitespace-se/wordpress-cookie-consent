{
  "name": "@whitespace/wordpress-cookie-consent",
  "version": "1.0.2",
  "main": "index.js",
  "author": "Whitespace Dev <code@whitespace.se>",
  "private": true,
  "devDependencies": {
    "@prettier/plugin-php": "^0.16.1",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.20.0",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.2.1"
  },
  "scripts": {
    "build": "parcel build 'assets/src/*.js' -d assets/dist --no-source-maps",
    "watch": "parcel watch 'assets/src/*.js' -d assets/dist",
    "format": "prettier --write '**/*.{js,json,md,yml,php,inc}'"
  },
  "dependencies": {
    "@whitespace/cookie-consent": "^3.1.0",
    "sendbeacon-polyfill": "^1.0.0"
  }
}
